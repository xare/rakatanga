import $ from 'jquery';

$().ready(function() {

    // ----- tabs -----

    /*     $('.nav-tabs a').click(function(e) {
            e.preventDefault();
            $(this).tab('show');
        });

        // ----- calcul réservations -----

        $('#reservation').on('change', 'select', function() {

            data = $('#reservation').serialize();
            $('#coordonnees').load('coordonnees/?' + data);
            $('#calcul').load('calcul/?' + data);

        });

        $('#reservation').on('click', '#appliquer', function() {

            data = $('#reservation').serialize();
            $('#coordonnees').load('coordonnees/?' + data);
            $('#calcul').load('calcul/?' + data);

        }); */



});