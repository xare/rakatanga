{{ form_start(form) }}
<div class="row">
    <div class="col-md-3 col-12">
        {{ form_widget(form) }}
        <button class="btn btn-primary">
            <i class="fa fa-edit"></i>
        </button>
    </div>
</div>
{{ form_end(form) }}
