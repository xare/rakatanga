{% extends 'admin/base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
<div class="container">
{{ include ('admin/shared/_controlband.html.twig',{'entity':'reservation'})}}
    <div class="row" data-container="js-admin-reservation-wrapper">
        <div class="col-md-12 col-12">
            <h1>Edit Reservation</h1>

            {{ include('admin/reservation/_form.html.twig', {'button_label': 'Update'}) }}
            <br />

            {{ include('admin/reservation/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('admin-send-message') }}
    {{ encore_entry_script_tags('notifications') }}
    {{ encore_entry_script_tags('admin-reservation') }}
{% endblock %}