{{ form_start(form) }}
    <div class="row">
        <div class="col-3">
            {{ form_widget(form) }}
            <button class="btn btn-primary">
                <i class="fa fa-edit"></i>
            </button>
        </div>
        <div class="col-9">
            {% if payment.id != null %}
                {{ include( 'admin/payments/_tabs_related.html.twig' )}}
            {% endif %}
        </div>
    </div>
{{ form_end(form) }}