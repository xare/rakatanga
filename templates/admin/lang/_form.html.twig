{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-info">
        <i class="fa fa-save"></i>
        {{ button_label|default('Guardar') }}
    </button>
{{ form_end(form) }}
