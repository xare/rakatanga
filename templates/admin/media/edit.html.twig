{% extends 'admin/base.html.twig' %}

{% block title %}Edit Media{% endblock %}

{% block body %}
<div class="container">
{{ include ('admin/shared/_controlband.html.twig',{'entity':'media'})}}
    <div class="row">
        <div class="col-md-12 col-xs-12">
        <h1>Edit Media</h1>
        <div class="row">
            <div class="col-md-4 col-xs-12">
            {{ include('admin/media/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
                <div class="col-md-6 col-xs-12">
                <img src="{{ medium.mediaPath|imagine_filter('squared_thumbnail_medium') }}" />
                </div>
            </div>
            <a href="{{ path('media_index') }}" class="btn btn-primary">
            <i class="fa fa-list"></i>
            Back to list</a>

            {{ include('admin/media/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
