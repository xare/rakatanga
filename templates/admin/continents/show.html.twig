{% extends 'admin/base.html.twig' %}

{% block title %}Continents{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12 col-sm-12">
            <h1>Continents</h1>
            <div class="table-responsive">
                <table class="table">
                    <tbody>
                        <tr>
                            <th>Code</th>
                            <td>{{ continent.code }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        <a href="{{ path('continents_index') }}">back to list</a>

        <a href="{{ path('continents_edit', {'id': continent.id}) }}">edit</a>
        {{ include('admin/continents/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
