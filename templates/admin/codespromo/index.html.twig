{% extends 'admin/base.html.twig' %}

{% block title %}Codespromo index{% endblock %}

{% block body %}
<div class="container">
    {{ include ('admin/shared/_controlband.html.twig',{'entity':'codespromo'}) }}
    {{ include ('admin/codespromo/partials/_filterband.html.twig') }}
    <div class="row">
        <div class="col-12">
            <h1>Codespromo index</h1>
            <div class="navigation">
                {{ knp_pagination_render(codespromos) }}
            </div>
            <div class="table-data">
                <div>
                    <table class="table table-striped table-hover small">
                        <thead>
                            <tr>
                                {# <th class="main-red">Id</th> #}
                                <th class="main-red">Code</th>
                                {# <th class="main-red">Libelle</th> #}
                                <th class="main-red">Commentaire</th>
                                <th class="main-red">€</th>
                                <th class="main-red">%</th>
                                <th class="main-red">Type</th>
                                <th class="main-red">Nombre</th>
                                <th class="main-red">Fechas</th>
                                <th class="main-red">Email</th>
                                <th class="main-red">Statut</th>
                                <th class="main-red">DateAjout</th>
                                <th class="main-red">User</th>
                                <th class="main-red">
                                    <i class="fa fa-trash"></i>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for codespromo in codespromos %}
                                {{include('admin/codespromo/_row.html.twig')}}
                            {% else %}
                                <tr>
                                    <td colspan="14">no records found</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="navigation mt-4">
                {{ knp_pagination_render(codespromos) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
