{% extends 'admin/base.html.twig' %}

{% block title %}MenuLocation{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>MenuLocation</h1>

                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ menu_location.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ menu_location.name }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>
                                <ul>
                                    {% for menu in menu_location.Menus %}
                                        <li>{{menu.title}}</li>
                                    {% else %}
                                        <li>No hay menús asignados</li> 
                                    {% endfor %}
                                </ul>
                            </td>
                        </tr>
                    </tbody>
                </table>

                <a href="{{ path('menu_location_index') }}">back to list</a>

                <a href="{{ path('menu_location_edit', {'id': menu_location.id}) }}">edit</a>

                {{ include('admin/menu_location/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
