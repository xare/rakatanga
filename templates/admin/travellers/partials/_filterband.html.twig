<fieldset class="filterband">
  <div class="startwrapper">
    <div class="total_items">
        <strong>Total de viajeros:</strong>
        <span id="totalItemCount">{{ count }}</span>
    </div>
  </div>
  <div class="endwrapper">
    <div class="pagination_selector">
        {{ include('admin/travellers/partials/_select_pagination_ammount.html.twig') }}
    </div>
    <div class="searchbox">
        {{ include('admin/travellers/partials/_search_results.html.twig') }}
    </div>
    {# <div class="results_filter">
        {{ include('admin/travellers/partials/_filtrar_resultados.html.twig') }}
    </div> #}
  </div>
</fieldset>