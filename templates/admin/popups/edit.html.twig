{% extends 'admin/base.html.twig' %}

{% block title %}Edit Popups{% endblock %}

{% block body %}
    <div class="container">
        {{ include ('admin/shared/_controlband.html.twig',{'entity':'popups'}) }}
        <div class="row">
            <div class="col-12">
                <h1>Edit Popups</h1>

                {{ include('admin/popups/_form.html.twig', {'button_label': 'Update'}) }}

                {{ include('admin/popups/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('datepicker') }}
    {{ encore_entry_script_tags('admin-media') }}

{% endblock %}
