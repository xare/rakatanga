{% extends 'admin/base.html.twig' %}

{% block title %}Popups index{% endblock %}

{% block body %}
    <div class="container">
    {{ include ('admin/shared/_controlband.html.twig',{'entity':'popups'}) }}
    {{ include ('admin/popups/partials/_filterband.html.twig') }}
        <div class="row">
            <div class="col-12">
                <h1>Popups index</h1>
                <div class="table-data">
                    <div>
                        <table class="table table-hover small">
                            <thead>
                                <tr>
                                    <th class="main-red">Id</th>
                                    <th class="main-red">Título</th>
                                    <th class="main-red">Content</th>
                                    <th class="main-red">Fecha Inicio</th>
                                    <th class="main-red">Fecha Final</th>
                                    <th class="main-red"><i class="fa fa-trash"></i></th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for popup in popups %}
                                {{ include('admin/popups/_row.html.twig') }}
                            {% else %}
                                <tr>
                                    <td colspan="4">no records found</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
