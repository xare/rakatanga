
<div class="alert alert-success">
  <p>{{'Ya has subido tu documento al sistema. Gracias. Para comprobar que es correcto puedes descargartelo aquí mismo'|trans}}.</p>
  <p>{{'Queda registrado tu pago de'|trans}} <strong>{{ammount|format_currency('EUR', {fraction_digit: 2}, locale=app.request.getLocale() )}}</strong></p>
  <p>{{ 'Gracias'|trans }}
  <hr>
  <ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <a
        href="{{ path("download_transfer_document",{
          'transferDocument': document.id}
        )}}"
        target="_blank"
      >
      {% if document.mimeType == 'image/jpeg'
      or document.mimeType == 'image/jpg'
      or document.mimeType == 'image/png' %}
        <img src="{{ path("download_transfer_document",{
          'transferDocument': document.id}
        )}}" style="width:50px; height:auto" />
      {% else %}
      <span>
            <span class="fa fa-download"></span>
        </span>
      {% endif %}

 <em>{{document.originalFilename}}</em> [{{document.mimetype}}]

      </a>
      {# <button class="btn btn-link btn-sm"
      data-action="js-document-delete"
        data-document-id="{{document.id}}"
        data-user-id="{{app.user.id}}"
        data-reservation-id="{{reservation.id}}">
        <span class="fa fa-trash"></span>
      </button> #}
    </li>
  </ul>
</div>