{% extends 'base.html.twig' %}

{% block title %}{{'Reinicia tu contraseña'|trans}}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-4 offset-md-4 col-12 card p-4">
            <h1>{{'Reinicia tu contraseña'|trans}}</h1>

            {{ form_start(resetForm) }}
                {{ form_row(resetForm.plainPassword,
                            {'label':'Tu contraseña'|trans}
                            ) }}
                <button class="btn btn-primary">{{'Reinicia tu contraseña'|trans}}</button>
            {{ form_end(resetForm) }}
        </div>
    </div>
</div>
{% endblock %}
