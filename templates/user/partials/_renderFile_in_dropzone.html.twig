
<div class="alert alert-success">
  <p>{{'Ya has subido tu documento al sistema. Gracias. Para comprobar que es correcto puedes descargartelo aquí mismo'|trans}}.</p>
  <hr>
  <ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <a
        href="{{ path("download_document",{
          'id': document.id}
        )}}"
        target="_blank"
      >
      {% if document.mimetype == 'image/jpeg'
      or document.mimetype == 'image/jpg'
      or document.mimetype == 'image/png' %}
        <img src="{{ path("download_document",{
          'id': document.id}
        )}}" style="width:50px; height:auto" />
      {% else %}
      <span>
            <span class="fa fa-download"></span>
        </span>
      {% endif %}

      <strong>{{document.doctype|trans}}:</strong> <em>{{document.originalFilename}}</em> [{{document.mimetype}}]

      </a>
      <button class="btn btn-link btn-sm"
      data-action="js-document-delete"
      data-type="{{document.doctype}}"
        data-document-id="{{document.id}}"
        {% if traveller is defined and traveller != null %}
          data-traveller-id="{{traveller.id}}"
        {% endif %}
        data-user-id="{{app.user.id}}"
        data-reservation-id="{{reservation.id}}">
        <span class="fa fa-trash"></span>
      </button>
    </li>
  </ul>
</div>