{% extends 'email/emailBase.html.twig' %}
{% block content %}
    <row class="welcome">
        <columns>
            <spacer size="35"></spacer>
            <h1>
                <center> Mensaje de {{contact.firstname}} <br />{{contact.lastname}} [{{contact.email}}] </center>
            </h1>
            <spacer size="10"></spacer>
        </columns>
    </row>
    <spacer size="30"></spacer>
    <row>
        <columns>
            <p>Un usuario ha rellenado el formulario de contacto.</p>
            <p><strong>Remitente:</strong> {{contact.firstname}} {{contact.lastname}} [<a href="mailto:{{contact.email}}">{{contact.email}}</a>].</p>
            <p><strong>Teléfono:</strong> {{contact.phone}}</p>
            <p><strong>Mensaje:</strong> {{contact.message}}.</p>
            <p><strong>Viajes por los que tengo interés:</strong></p> 
            <ul>
            {% for travel in travels %}
                <li>{{ travel.title }}</li>
            {% endfor %}
            </ul>
        </columns>
    </row>
{% endblock %}