{% extends 'email/emailBase.html.twig' %}
{% block content %}
    <row class="welcome">
        <columns>
            <spacer size="35"></spacer>
            <h1>
                <center>Nuevo usuario: {{user.prenom}} {{user.nom}} [{{user.email}}]</center>
            </h1>
            <spacer size="10"></spacer>
        </columns>
    </row>
    <spacer size="30"></spacer>
    <row>
        <columns>
            <p><strong>Remitente:</strong> {{user.prenom}} {{user.nom}} [<a href="mailto:{{user.email}}">{{user.email}}</a>].</p>
        </columns>
    </row>
    <row>
        <columns>
            <p><strong>Teléfono:</strong> {{user.telephone}}</p>
        </columns>
    </row>
    {# <row>
        <columns>
            <p><strong>Estado del registro:</strong> {{user.verification}}.</p>
        </columns>
    </row> #}
{% endblock %}